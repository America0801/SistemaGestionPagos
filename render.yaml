services:
  - type: web
    name: sistema-gestion-pagos
    env: dotnet
    buildCommand: "dotnet publish ./SistemaGestionPagos.Web/SistemaGestionPagos.Web.csproj -c Release -o out"
    startCommand: "dotnet out/SistemaGestionPagos.Web.dll --urls=http://0.0.0.0:$PORT"
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ConnectionStrings__DefaultConnection
        sync: false   # la defines manualmente en Render
